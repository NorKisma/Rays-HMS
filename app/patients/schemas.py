# Schema for validation (e.g., Marshmallow)
# from marshmallow import Schema, fields

# class PatientSchema(Schema):
#     id = fields.Int(dump_only=True)
#     first_name = fields.Str(required=True)
#     ...
